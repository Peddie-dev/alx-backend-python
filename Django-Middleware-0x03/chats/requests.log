import os

log_file = 'requests.log'

if os.path.exists(log_file) and os.path.getsize(log_file) > 0:
    print(f"'{log_file}' exists and is not empty.")
else:
    print(f"'{log_file}' either does not exist or is empty.")
